# Sanitisation status

This tree was prepared as a clean public-source copy for `blondothenerd/prayr`.

Completed checks:

- public namespace/application ID applied;
- Java source package moved to match the public namespace;
- file-provider authority and internal action names updated;
- previous signing material removed from the source tree;
- local/release signing credentials excluded by `.gitignore`;
- public repository metadata and MIT licence added;
- text scan performed for private organisation/domain/email/network/path markers;
- common credential/token patterns scanned;
- no Git history included.

Before publishing, add a screenshot using sample content and run the checklist in `docs/PUBLIC_RELEASE_CHECKLIST.md`.
